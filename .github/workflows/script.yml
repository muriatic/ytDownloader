name: Unittesting

on:
    push:
        paths:
            - '**.py'
        branches: ["main"]
    
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest
        
        steps:
            - uses: actions/checkout@v3
            - name: Unittesting
              run: python ../.tools/test_yt_downloader.py